export default (editor, config = {}) => {
  const blockManager = editor.BlockManager;

  // Here you can start interacting with BlockManager (https://github.com/artf/grapesjs/wiki/API-Block-Manager)
};
