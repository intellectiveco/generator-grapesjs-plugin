export default (editor, config = {}) => {
  const styleManager = editor.StyleManager;
  const customStyleManager = config.customStyleManager;

  // Here you can start interacting with Styles (https://github.com/artf/grapesjs/wiki/API-Style-Manager)
};
